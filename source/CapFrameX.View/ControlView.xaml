<UserControl x:Class="CapFrameX.View.ControlView"
             Name="ControlViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			 xmlns:prism="http://prismlibrary.com/"
			 xmlns:dragdrop="urn:gong-wpf-dragdrop"
             xmlns:mvvm="clr-namespace:CapFrameX.MVVM.Converter;assembly=CapFrameX.MVVM"
             xmlns:controls="clr-namespace:CapFrameX.View.Controls"	
			 xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
			 xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <ResourceDictionary>
            <CollectionViewSource x:Key="RecordInfoListKey" Source="{Binding RecordInfoList}" Filter="RecordInfoListOnFilter"/>
            <mvvm:AddValueConverter x:Key="AddValueConverterExpander" Offset="-70"/>
            <mvvm:BoolToVisibilityConverter x:Key="FalseToVisibleConverter" FalseValue="Visible" TrueValue="Collapsed"/>
            <mvvm:PathLeafConverter x:Key="PathLeafConverter"/>
            <mvvm:BoolToVisibilityConverter x:Key="TrueToVisibleConverter" FalseValue="Collapsed" TrueValue="Visible"/>
            <mvvm:BoolToVisibilityConverter x:Key="FalseToCollapsedConverter" FalseValue="Collapsed" TrueValue="Visible"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <FrameworkElement x:Name="ColumnHeaders" Visibility="Collapsed"/>
<!-- Observed-directory tree moved into the breadcrumb popup below (no layout reflow). -->
        <!--Record list. The observed-directory drawer overlays this area when it is
             opened transiently; only pinning it docks the drawer and shifts the list. -->
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Border Grid.Column="0" Grid.Row="0" Visibility="{Binding HasValidSource, Converter={StaticResource FalseToVisibleConverter}}" Margin="0 0 50 0" BorderThickness="1" Panel.ZIndex="2">
                <!--Style="{StaticResource MaterialDesignHeadlineTextBlock}"-->
                <TextBlock Text="Observed directory missing.&#10;Open the folder chip above and select a folder." TextAlignment="Center"
                       TextWrapping="Wrap" Width="220" Margin="60 0 10 0" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            </Border>
            <DockPanel Margin="5 0 0 0"  VerticalAlignment="Stretch" >
                <Grid Grid.Row="0" Visibility="{Binding HasValidSource, Converter={StaticResource FalseToCollapsedConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="1*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Text="Loading records..." FontSize="16" Panel.ZIndex="1" Visibility="{Binding DirectoryLoading, Converter={StaticResource TrueToVisibleConverter}}"
                        Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
					<TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Text="Directory is empty. Refresh directory (context menu) or start a game and then press the capture key to create records." TextWrapping="Wrap" Width="200" TextAlignment="Center" FontSize="16" Panel.ZIndex="1"
                        Visibility="{Binding DirectoryIsEmpty, Converter={StaticResource TrueToVisibleConverter}}"
                        Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>

                    <!--Column options & search bar-->
                    <DockPanel Grid.Row="0" Margin="8 8 8 4" LastChildFill="True">
                        <!--Observed-directory breadcrumb: opens the folder tree in a floating
                            popup that overlays nothing in the layout, so the record list is
                            never reflowed. Replaces the old side expander/drawer. -->
                        <Button x:Name="FolderChipButton" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0 0 12 0"
                            Click="FolderChipButton_Click" Cursor="Hand" Height="32" Padding="10 0"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                            ToolTip="{Binding DataContext.RootDirectory, ElementName=ControlViewControl}" ToolTipService.ShowDuration="25000">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FolderOutline" Width="18" Height="18" VerticalAlignment="Center"/>
                                <TextBlock Margin="8 0 4 0" VerticalAlignment="Center" MaxWidth="180" TextTrimming="CharacterEllipsis"
                                    Text="{Binding DataContext.RootDirectory, ElementName=ControlViewControl, Converter={StaticResource PathLeafConverter}}"/>
                                <materialDesign:PackIcon Kind="ChevronDown" Width="16" Height="16" VerticalAlignment="Center" Opacity="0.7"/>
                            </StackPanel>
                        </Button>
                        <Popup x:Name="FolderPopup" DockPanel.Dock="Left" PlacementTarget="{Binding ElementName=FolderChipButton}" Placement="Bottom"
                            StaysOpen="False" AllowsTransparency="True" PopupAnimation="Fade" HorizontalOffset="0" VerticalOffset="4">
                            <materialDesign:Card Width="330" Margin="8" UniformCornerRadius="6"
                                Background="{DynamicResource MaterialDesign.Brush.TextBox.FilledBackground}">
                                <materialDesign:Card.Effect>
                                    <DropShadowEffect BlurRadius="16" ShadowDepth="3" Opacity="0.5"/>
                                </materialDesign:Card.Effect>
                                <materialDesign:DialogHost IsOpen="{Binding CreateFolderDialogIsOpen}"
                                    DialogContent="{Binding CreateFolderDialogContent}" CloseOnClickAway="False">
                                    <Grid Margin="12">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <DockPanel Grid.Row="0" LastChildFill="True">
                                            <Button DockPanel.Dock="Right" VerticalAlignment="Bottom" Margin="8 0 0 4"
                                                Width="26" Height="26" Padding="0" Style="{StaticResource MaterialDesignIconButton}"
                                                ToolTip="Reload root folder" Command="{Binding ReloadRootFolderCommand}"
                                                Foreground="{DynamicResource MaterialDesign.Brush.ToolBar.Item.Background}">
                                                <materialDesign:PackIcon Kind="Cached" Width="18" Height="18"/>
                                            </Button>
                                            <TextBox IsReadOnly="True" materialDesign:HintAssist.Hint="Root capture folder"
                                                materialDesign:HintAssist.IsFloating="True" materialDesign:HintAssist.HintOpacity=".8"
                                                Text="{Binding DataContext.RootDirectory, ElementName=ControlViewControl, Mode=OneWay}"
                                                TextWrapping="NoWrap" PreviewMouseDown="RootFolder_PreviewMouseDown"
                                                ToolTip="{Binding DataContext.RootDirectory, ElementName=ControlViewControl, Mode=OneWay}"/>
                                        </DockPanel>
                                        <ScrollViewer Grid.Row="1" Margin="0 12 0 0" Height="320" VerticalScrollBarVisibility="Auto"
                                            PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                            <TreeView Name="TrvStructure" Padding="5" TreeViewItem.Selected="TreeViewItem_Selected"
                                                PreviewMouseRightButtonDown="TreeView_PreviewMouseRightButtonDown"
                                                Tag="{Binding ElementName=ControlViewControl, Path=DataContext}" FontSize="13"
                                                AllowDrop="True" DragOver="TrvStructure_DragOver" MouseEnter="TrvStructure_MouseEnter"
                                                dragdrop:DragDrop.IsDragSource="True" dragdrop:DragDrop.UseDefaultDragAdorner="True"
                                                dragdrop:DragDrop.DefaultDragAdornerOpacity="0.5"
                                                dragdrop:DragDrop.UseDefaultEffectDataTemplate="False" Drop="TrvStructure_Drop">
                                                <TreeView.ContextMenu>
                                                    <ContextMenu DataContext="{Binding Path=PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                                        <MenuItem Header="Open folder" Command="{Binding OpenObservedFolderCommand}"/>
                                                        <MenuItem Header="Delete folder" Command="{Binding DeleteFolderCommand}"/>
                                                        <MenuItem Header="Create new subfolder" Command="{Binding OpenCreateSubFolderDialogCommand}"/>
                                                    </ContextMenu>
                                                </TreeView.ContextMenu>
                                            </TreeView>
                                        </ScrollViewer>
                                    </Grid>
                                </materialDesign:DialogHost>
                            </materialDesign:Card>
                        </Popup>
                        <materialDesign:PackIcon DockPanel.Dock="Right" Kind="Magnify" Width="20" Height="20"
                            Margin="8 0 4 4" VerticalAlignment="Center" Opacity="0.6"
                            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                        <materialDesign:PopupBox x:Name="ColumnOptionsPopup" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0 0 8 0" StaysOpen="True" ToolTipService.ShowDuration="25000"
                            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}">
                        <materialDesign:PopupBox.ToolTip>
                            <ToolTip Placement="Top">
                                <TextBlock Text="Column options"/>
                            </ToolTip>
                        </materialDesign:PopupBox.ToolTip>
                        <materialDesign:Card Width="Auto" Margin="0 0 0 -10">
                            <Grid Margin="8 8 8 8">
                                <Grid.Resources>
                                    <Style TargetType="{x:Type CheckBox}"
											BasedOn="{StaticResource MaterialDesignCheckBox}">
                                        <Setter Property="Margin" Value="8,8,10,8" />
                                    </Style>
                                    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignTextBox}">
                                        <Setter Property="materialDesign:HintAssist.IsFloating" Value="True" />
                                        <Setter Property="Margin" Value="8,8,8,8" />
                                    </Style>
                                </Grid.Resources>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Margin="5" Grid.Row="0" Grid.Column="0" Text="Date / Time" VerticalAlignment="Center"/>
                                <ToggleButton Margin="5" Grid.Row="0" Grid.Column="1" IsChecked="{Binding ShowCreationDate}" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                                <TextBlock Margin="5" Grid.Row="1" Grid.Column="0" Text="Comment" VerticalAlignment="Center"/>
                                <ToggleButton Margin="5" Grid.Row="1" Grid.Column="1" IsChecked="{Binding ShowComment}" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                                <TextBlock Margin="5" Grid.Row="2" Grid.Column="0" Text="Aggregated" VerticalAlignment="Center"/>
                                <ToggleButton Margin="5" Grid.Row="2" Grid.Column="1" IsChecked="{Binding ShowIsAggregated}" Style="{StaticResource MaterialDesignSwitchToggleButton}" />
                                <TextBlock Margin="5" Grid.Row="3" Grid.Column="0" Text="CPU" VerticalAlignment="Center"/>
                                <ToggleButton Margin="5" Grid.Row="3" Grid.Column="1" IsChecked="{Binding ShowCpuName}" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                                <TextBlock Margin="5" Grid.Row="4" Grid.Column="0" Text="GPU" VerticalAlignment="Center"/>
                                <ToggleButton Margin="5" Grid.Row="4" Grid.Column="1" IsChecked="{Binding ShowGpuName}" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                                <TextBlock Margin="5" Grid.Row="5" Grid.Column="0" Text="RAM" VerticalAlignment="Center"/>
                                <ToggleButton Margin="5" Grid.Row="5" Grid.Column="1" IsChecked="{Binding ShowRamName}" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                            </Grid>
                        </materialDesign:Card>
                    </materialDesign:PopupBox>
                        <TextBox Name="RecordSearchBox" VerticalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                            materialDesign:HintAssist.Hint="{Binding SearchBarText}"
                            materialDesign:HintAssist.HintOpacity=".8"/>
                    </DockPanel>

                    <!--Datagrid-->
                    <controls:MultiSelectionDataGrid Grid.Row="1" x:Name="RecordDataGrid" Height="Auto"
                            Margin="8 4 8 0" Sorting="RecordDataGrid_Sorting" SelectionMode="Extended" SelectedIndex="{Binding RecordDataGridSelectedIndex}"
					        Tag="{Binding ElementName=ControlViewControl, Path=DataContext}" FontSize="12"
					        ItemsSource="{Binding Source={StaticResource RecordInfoListKey}}"
                            SelectedItemsList="{Binding SelectedRecordInfos, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            SelectedItem="{Binding SelectedRecordInfo}" CanUserSortColumns="True" CanUserAddRows="False" AutoGenerateColumns="False"
                            PreviewLostKeyboardFocus="RecordDataGrid_PreviewLostKeyboardFocus"
							PreviewKeyDown="RecordDataGrid_PreviewKeyDown"
                            BeginningEdit="RecordDataGrid_BeginningEdit"
                            ColumnDisplayIndexChanged="RecordDataGrid_ColumnDisplayIndexChanged"
					        materialDesign:DataGridAssist.CellPadding="12 6 12 6" materialDesign:DataGridAssist.ColumnHeaderPadding="12 6 12 6" IsSynchronizedWithCurrentItem="False"
					        ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.CanContentScroll="True"
					        dragdrop:DragDrop.IsDragSource="True"
					        dragdrop:DragDrop.IsDropTarget="False"
					        dragdrop:DragDrop.UseDefaultDragAdorner="True"
					        dragdrop:DragDrop.DefaultDragAdornerOpacity="0.5"
                            dragdrop:DragDrop.UseDefaultEffectDataTemplate="False"
                            Style="{StaticResource MaterialDesignDataGrid}"
                            EnableRowVirtualization="True"
                            EnableColumnVirtualization="True"
                            VirtualizingPanel.IsVirtualizing="True"
                            VirtualizingPanel.VirtualizationMode="Recycling"
                            Background="{DynamicResource MaterialDesign.Brush.Card.Background}">
                        <DataGrid.Resources>
                            <Style TargetType="{x:Type DataGridRow}">
                                <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                                <Setter Property="DataGrid.Height" Value="36" />
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="SnapsToDevicePixels" Value="true"/>
                                <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
                                <Setter Property="ValidationErrorTemplate">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <TextBlock Foreground="#d50000" Margin="2,0,0,0" Text="!" VerticalAlignment="Center"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type DataGridRow}">
                                            <Border x:Name="DGR_Border"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Background="{TemplateBinding Background}"                                                                  
                                                SnapsToDevicePixels="True">
                                                <SelectiveScrollingGrid>
                                                    <SelectiveScrollingGrid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </SelectiveScrollingGrid.ColumnDefinitions>
                                                    <SelectiveScrollingGrid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </SelectiveScrollingGrid.RowDefinitions>
                                                    <DataGridCellsPresenter Grid.Column="1"
                                                                        ItemsPanel="{TemplateBinding ItemsPanel}"
                                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                    <DataGridDetailsPresenter Grid.Column="1"
                                                                        Grid.Row="1"
                                                                        SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                                        Visibility="{TemplateBinding DetailsVisibility}" />
                                                    <DataGridRowHeader Grid.Column="0"
                                                                    Grid.Row="0"
                                                                    Grid.RowSpan="2"
                                                                    SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                                                                    Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                                                </SelectiveScrollingGrid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.DataGrid.RowHoverBackground}" />
                                    </Trigger>
                                    <Trigger Property="IsNewItem" Value="True">
                                        <Setter Property="Margin" Value="{Binding NewItemMargin, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                            <Condition Property="IsMouseOver" Value="True" />
                                        </MultiTrigger.Conditions>
                                        <MultiTrigger.Setters>
                                            <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}" />
                                        </MultiTrigger.Setters>
                                    </MultiTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding SelectedRecordingsCommand}" 
                               CommandParameter="{Binding ElementName=RecordDataGrid, Path=SelectedItems}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.InputBindings>
                            <KeyBinding Key="Delete" Command="{Binding Path=DeleteRecordCommand}" />
                        </DataGrid.InputBindings>
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding Path=PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Delete recording file(s)" Command="{Binding DeleteRecordFileCommand}"/>
                                <MenuItem Header="Move recording file(s)" Command="{Binding MoveRecordFileCommand}"/>
                                <MenuItem Header="Copy/paste recording file(s)" Command="{Binding DuplicateRecordFileCommand}"/>
								<MenuItem Header="Refresh directory" Command="{Binding RefreshDirectoryCommand}"/>
							</ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="BorderThickness" Value="0, 0, 1, 0"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}" />
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="TextBlock.FontSize" Value="12" />
                                <Setter Property="TextBlock.FontWeight" Value="Medium" />
                                <Setter Property="Padding" Value="12, 5, 0, 0"/>
                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Header.Foreground}" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                                            <ControlTemplate.Resources>
                                                <Storyboard x:Key="GoToVisible">
                                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SortDirectionIndicator" Storyboard.TargetProperty="Width">
                                                        <EasingDoubleKeyFrame KeyTime="0:0:0.0" Value="0" />
                                                        <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="12" />
                                                    </DoubleAnimationUsingKeyFrames>
                                                </Storyboard>
                                                <Storyboard x:Key="GoToHidden">
                                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SortDirectionIndicator" Storyboard.TargetProperty="Width">
                                                        <EasingDoubleKeyFrame KeyTime="0:0:0.0" Value="12" />
                                                        <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="0" />
                                                    </DoubleAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </ControlTemplate.Resources>
                                            <Grid>
                                                <Border Margin="0 5 0 5" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                    <Grid Background="Transparent">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <wpf:ListSortDirectionIndicator Grid.Column="0" Width="0" Height="14" 
                                                                x:Name="SortDirectionIndicator"
                                                                ListSortDirection="{TemplateBinding SortDirection}"/>
                                                        <ContentPresenter Height="20" Grid.Column="1" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"/>
                                                    </Grid>
                                                </Border>
                                                <Thumb x:Name="PART_LeftHeaderGripper" HorizontalAlignment="Left" Style="{StaticResource MaterialDesignGridColumnHeaderGripper}"/>
                                                <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Style="{StaticResource MaterialDesignGridColumnHeaderGripper}"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
                                                </Trigger>
                                                <Trigger Property="SortDirection" Value="Ascending">
                                                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
                                                </Trigger>
                                                <Trigger Property="SortDirection" Value="Descending">
                                                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
                                                </Trigger>
                                                <Trigger SourceName="SortDirectionIndicator" Property="IsNeutral" Value="False">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard Storyboard="{StaticResource GoToVisible}" Name="GoToVisible" />
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <RemoveStoryboard BeginStoryboardName="GoToVisible" />
                                                        <BeginStoryboard Storyboard="{StaticResource GoToHidden}" />
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding GameName, Mode=OneWay}"
                                        Width="Auto"
                                        Header="Game"
                                        FontSize="13"
                                        IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style>
                                        <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding CreationTimestamp, Mode=OneWay}"
                                        Width="Auto"
                                        MinWidth="95"
                                        Header="Date / Time"
                                        IsReadOnly="True"
                                        Visibility="{Binding DataContext.ShowCreationDate, Source={x:Reference ColumnHeaders}, Converter={StaticResource TrueToVisibleConverter}}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style>
                                        <Setter Property="TextBlock.TextAlignment" Value="Center" />
                                        <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding Comment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
										Width="100"
                                        Header="Comment"
                                        IsReadOnly="False"
                                        Visibility="{Binding DataContext.ShowComment, Source={x:Reference ColumnHeaders}, Converter={StaticResource TrueToVisibleConverter}}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style>
                                        <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridCheckBoxColumn Binding="{Binding IsAggregated, Mode=OneWay}"
                                        Width="Auto"
                                        Header="Aggregated"
                                        IsReadOnly="True" 
                                        MinWidth="40"
                                        Visibility="{Binding DataContext.ShowIsAggregated, Source={x:Reference ColumnHeaders}, Converter={StaticResource TrueToVisibleConverter}}">
                                <DataGridCheckBoxColumn.ElementStyle>
                                    <Style>
                                        <Setter Property="CheckBox.IsEnabled" Value="False" />
                                        <Setter Property="CheckBox.VerticalAlignment" Value="Center" />
                                        <Setter Property="CheckBox.HorizontalAlignment" Value="Center" />
                                        <Setter Property="CheckBox.IsHitTestVisible" Value="False"/>
                                    </Style>
                                </DataGridCheckBoxColumn.ElementStyle>
                            </DataGridCheckBoxColumn>
                            <DataGridTextColumn Binding="{Binding ProcessorName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
										Width="Auto"
                                        Header="CPU"
                                        IsReadOnly="False"
                                        Visibility="{Binding DataContext.ShowCpuName, Source={x:Reference ColumnHeaders}, Converter={StaticResource TrueToVisibleConverter}}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style>
                                        <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding GraphicCardName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
										Width="Auto"
                                        Header="GPU"
                                        IsReadOnly="False"
                                        Visibility="{Binding DataContext.ShowGpuName, Source={x:Reference ColumnHeaders}, Converter={StaticResource TrueToVisibleConverter}}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style>
                                        <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
                                    </Style> 
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding SystemRamInfo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
										Width="Auto"
                                        Header="RAM"
                                        IsReadOnly="False"
                                        Visibility="{Binding DataContext.ShowRamName, Source={x:Reference ColumnHeaders}, Converter={StaticResource TrueToVisibleConverter}}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style>
                                        <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </controls:MultiSelectionDataGrid>
                </Grid>
            </DockPanel>
            <Expander Name="RecordInfoExpander" Grid.Row="1" Grid.Column="1" Margin="8 4 8 8" VerticalAlignment="Bottom" Panel.ZIndex="1" ExpandDirection="Down"
                      IsExpanded="False" wpf:ExpanderAssist.HorizontalHeaderPadding="12 8 12 8" Expanded="RecordInfoExpander_Change" Collapsed="RecordInfoExpander_Change"
                      Foreground="{DynamicResource MaterialDesign.Brush.Foreground}" Background="{DynamicResource MaterialDesign.Brush.TextBox.FilledBackground}">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CardTextOutline" Width="18" Height="18" VerticalAlignment="Center" Margin="0 0 8 0"/>
                        <TextBlock Text="Record Info" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SelectedRecordInfo.GameName, StringFormat=' — {0}'}" FontSize="14" Opacity="0.7" VerticalAlignment="Center"/>
                    </StackPanel>
                </Expander.Header>
                <Grid Margin="12 4 12 12">
                    <Grid.Resources>
                        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Margin" Value="0 4 0 4"/>
                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                            <Setter Property="wpf:HintAssist.HintOpacity" Value=".8"/>
                        </Style>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <!--System descriptions (left column)-->
                    <TextBox Grid.Row="0" Grid.Column="0" wpf:HintAssist.Hint="CPU description"
                             Text="{Binding CustomCpuDescription, UpdateSourceTrigger=PropertyChanged}" KeyDown="TextBox_KeyDown"/>
                    <TextBox Grid.Row="1" Grid.Column="0" wpf:HintAssist.Hint="GPU description"
                             Text="{Binding CustomGpuDescription, UpdateSourceTrigger=PropertyChanged}" KeyDown="TextBox_KeyDown"/>
                    <TextBox Grid.Row="2" Grid.Column="0" wpf:HintAssist.Hint="RAM description"
                             Text="{Binding CustomRamDescription, UpdateSourceTrigger=PropertyChanged}" KeyDown="TextBox_KeyDown"/>
                    <!--Record metadata (right column)-->
                    <TextBox Grid.Row="0" Grid.Column="2" wpf:HintAssist.Hint="Game"
                             Text="{Binding CustomGameName, UpdateSourceTrigger=PropertyChanged}" KeyDown="TextBox_KeyDown"/>
                    <TextBox Grid.Row="1" Grid.Column="2" Grid.RowSpan="2" VerticalAlignment="Stretch" wpf:HintAssist.Hint="Comment"
                             Text="{Binding CustomComment, UpdateSourceTrigger=PropertyChanged}" KeyDown="TextBox_KeyDown"/>
                    <!--Actions-->
                    <Button Grid.Row="3" Grid.Column="2" Margin="0 12 0 0" MinWidth="120" HorizontalAlignment="Right"
                            Style="{DynamicResource MaterialDesignRaisedButton}" Command="{Binding AcceptEditingDialogCommand}"
                            ToolTip="Apply the descriptions to the selected record(s). Enter inside a field saves as well.">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" VerticalAlignment="Center" Margin="0 0 8 0"/>
                            <TextBlock Text="SAVE" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Expander>
        </Grid>
    </Grid>
</UserControl>
