<Window x:Class="CapFrameX.Shell"
        xmlns:prism="http://www.codeplex.com/prism"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"	   
        mc:Ignorable="d"
		Title="CapFrameX"
        Height="780" Width="1280" Icon="Icons/CX_Icon.png">
	<Grid>
		<DockPanel>
			<ContentControl Name="Colorbar" DockPanel.Dock="Top" prism:RegionManager.RegionName="ColorbarRegion" />
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="1*" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="1*"></RowDefinition>
					<RowDefinition Height="30"></RowDefinition>
				</Grid.RowDefinitions>
				<ContentControl Grid.Column="0" Grid.Row="0" Name="Control" prism:RegionManager.RegionName="ControlRegion" />
				<ContentControl Grid.Column="1" Grid.Row="0" Name="Data" prism:RegionManager.RegionName="DataRegion" />
				<ContentControl Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Name="State" prism:RegionManager.RegionName="StateRegion" />
			</Grid>
		</DockPanel>
	</Grid>
</Window>
